# QuestMe

Expo SDK 54 app with a start screen, swipe-to-register entry, and a registration form.

## Run

```bash
npm install
npm run start
```

Platform shortcuts:

```bash
npm run android
npm run ios
npm run web
```
