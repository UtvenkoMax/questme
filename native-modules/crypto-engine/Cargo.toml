[package]
name = "questme-crypto-engine"
version = "0.1.0"
edition = "2021"
description = "Cryptographic operations for QuestMe"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"
base64 = "0.22"
